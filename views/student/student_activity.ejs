<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Activity</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/student/css/header.css">
    <style>
        body {
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .content {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-top: 20px;
        }
        .left-section {
            flex: 1;
            font-size: 30px;
            padding-left: 30px;
            padding-top: 20px;
        }
        .task-card {
            font-size: 20px; /* Adjust text size */
            background: #a7c7e7;
            padding: 30px;
            border-radius: 8px;
            color: rgb(0, 0, 0);
            flex: 1; /* Allows the card to expand flexibly within its container */
            margin-top: 30px;
            width: 80%; /* Increased width */
            box-sizing: border-box; /* Ensures padding doesn't affect width calculation */
        }
        .task-card a {
            color: white;
            text-decoration: none;
            font-size: 20px;
        }
        .task-card a:hover {
            text-decoration: underline;
        }

        .whiteboard-launch {
            margin: 0; /* Reset margin */
            align-self: flex-start; /* Align with top of task card */
        }

        .whiteboard-launch .btn-primary {
            background-color: #29649f; /* Matches the header and task card theme */
            border: none; /* Removes border for a cleaner look */
            border-radius: 5px; /* Rounded corners */
            padding: 10px 20px; /* Adds comfortable spacing */
            font-size: 16px; /* Adjusts font size */
            font-weight: bold; /* Makes the text stand out */
            color: white; /* Text color */
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.2); /* Adds a subtle shadow */
            transition: background-color 0.3s ease, transform 0.2s ease; /* Smooth hover effects */
        }

        .whiteboard-launch .btn-primary:hover {
            background-color: #86add9; /* Slightly darker shade on hover */
            transform: scale(1.05); /* Enlarges button slightly when hovered */
            cursor: pointer; /* Changes cursor to a pointer */
        }

    </style>
</head>
<body>
    <div class="dashboard container-fluid">
        <div class="header">
            <button class="btn btn-light" id="back" onclick="history.back()">&#8592;</button>
            <div class="nav-links">
                <div id="content">Content</div>
                <div id="submissions">Submissions</div>
                <div id="feedback">Feedback</div>
            </div>
            <div class="dropdown">
                <button class="btn btn-light dropdown-toggle" type="button" id="settingsDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    ⚙️
                </button>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="settingsDropdown">
                    <li><a class="dropdown-item" href="#" id="logout">Logout</a></li>
                </ul>
            </div>
        </div>
        <div class="content">
            <div class="left-section">
                <h2><i>Activity!</i></h2>
            
                <div class="task-container">
                    <div class="task-card">
                        <img src="/graphics/welcome.png" alt="Assignment Image" style="width: 350px; height: auto;">
                        <p><br> Hello! Welcome to your first day of classes. Let's start with something fun.<br>
                            <br>1. Tell me about yourself. Where you are from, your hobbies and your favourite cartoon.<br>
                            2. Can you draw or colour a picture of your favourite cartoon?<br>
                            3. You can start of by trying out the Whiteboard: Your Creative Space.
                        </p>
                    </div>
            
                    <!-- Whiteboard Button now inline with task-card -->
                    <div id="openWhiteboard" class="whiteboard-launch">
                        <button class="btn btn-primary">Whiteboard: Your Creative Space</button>
                    </div>
                </div>
            </div>
            
    <script>
        function handlePageShow(event) {
            if (event.persisted) {
                checkTokenLocalStorage();
            }
        }

        function checkTokenLocalStorage() {
            if (!localStorage.getItem('token')) {
                window.location.href = '/login';
            }
        }

        window.addEventListener('pageshow', handlePageShow);

        document.getElementById("content").addEventListener("click", function() {
            window.location.href = "/student_dashboard/<%= subjectName %>";
        });
        document.getElementById("submissions").addEventListener("click", function() {
            window.location.href = "/student_dashboard/<%= subjectName %>/activity/submission";
        });
        document.getElementById("feedback").addEventListener("click", function() {
            window.location.href = "/student_dashboard/<%= subjectName %>/activity/submission/feedback";
        });
        document.getElementById("back").addEventListener("click", function() {
            window.location.href = "/student_dashboard/<%= subjectName %>";
        });
        document.getElementById("logout").addEventListener("click", function() {
            localStorage.removeItem('token');
            window.location.href = "/login";
        });

        // Whiteboard button click
        document.getElementById("openWhiteboard").addEventListener("click", function() {
            window.location.href = "/student/whiteboard";
        });
    </script>
</body>
</html>
